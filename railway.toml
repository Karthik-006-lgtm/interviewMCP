[build]
builder = "NIXPACKS"
watchPatterns = ["**/*.java", "**/*.ts", "**/*.tsx", "**/*.py"]

[deploy]
startCommand = "echo 'Deploy each service separately in Railway dashboard'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
